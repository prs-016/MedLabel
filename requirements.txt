# Streamlit Community Cloud deployment
# Heavy ML packages (PaddleOCR, YOLO, FlagEmbedding) are excluded here — they exceed
# Cloud's resource limits. Use requirements-full.txt for local full-feature installs.

numpy>=1.21,<3
pillow
pillow-heif

# AI & LLM
openai>=1.0.0                     # xAI / Grok API client (MedBrain)

# RAG & Vector DB
chromadb>=0.4.0                   # Vector store
sentence-transformers>=2.2.0      # Cross-encoder reranker

# Web scraping (DDInter client)
beautifulsoup4>=4.12.0
requests>=2.31.0

# Application
streamlit>=1.30.0        # UI Dashboard
python-dotenv            # API key management
pandas                   # Data handling
pydantic>=2.0.0          # Data validation
