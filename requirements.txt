# Streamlit Community Cloud deployment

numpy>=1.21,<3
pillow
pillow-heif

# OCR
easyocr>=1.7.0           # Flat label OCR (sentence-transformers already pulls torch)
ultralytics>=8.3.0       # YOLO geometry detection (torch already present via sentence-transformers)

# AI & LLM
openai>=1.0.0            # xAI / Grok API client (MedBrain)

# RAG & Vector DB
chromadb>=0.4.0          # Vector store
sentence-transformers>=2.2.0  # Cross-encoder reranker (also provides torch)

# Web scraping (DDInter client)
beautifulsoup4>=4.12.0
requests>=2.31.0

# Application
streamlit>=1.30.0        # UI Dashboard
python-dotenv            # API key management
pandas                   # Data handling
pydantic>=2.0.0          # Data validation
