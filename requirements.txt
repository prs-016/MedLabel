# Path A (vision): OpenCV + PaddleOCR
# PaddleOCR does NOT install PaddlePaddle (`import paddle`). Install it second:
#   python -m pip install -r requirements-paddle.txt
numpy>=1.21,<3
opencv-python-headless>=4.8,<5
paddleocr>=2.7,<4

# Computer Vision
ultralytics>=8.3.0       # YOLOv11
pillow                   # Image handling

# AI & LLM
google-generativeai>=0.3.0        # Gemini 2.5 Flash (Path B OCR + Simplifier)
openai>=1.0.0                     # xAI / Grok API client (MedBrain)
rank-bm25>=0.2.2                  # BM25 keyword ranking in MedBrain

# RAG & Vector DB
chromadb>=0.4.0                   # Vector store
sentence-transformers>=2.2.0      # BGE-M3 embeddings + CrossEncoder reranker
FlagEmbedding>=1.2.0              # BGE-M3 FlagModel + FlagReranker (optional but preferred)

# Web scraping (DDInter client)
beautifulsoup4>=4.12.0
requests>=2.31.0

# Roboflow hosted inference (optional router)
inference-sdk>=0.9.0

# Application
streamlit>=1.30.0        # UI Dashboard
python-dotenv            # API key management
pandas                   # Data handling
pydantic>=2.0.0          # Data validation
