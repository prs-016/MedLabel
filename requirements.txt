# Environment for MedLabel (optimized for Apple Silicon / M4 Pro)

# Computer Vision & OCR
ultralytics>=8.3.0        # YOLOv11
paddleocr>=2.7,<3       # Path A (flat labels)
# PaddleOCR does not install PaddlePaddle (`import paddle`). After this file run:
#   python -m pip install -r requirements-paddle.txt
opencv-python-headless>=4.8,<5
pillow                  # Image handling
numpy>=1.21,<3

# AI & LLM
google-generativeai>=0.3.0  # Gemini 2.0 Flash
langchain>=0.1.0            # Agentic framework
langchain-google-genai>=1.0.0
chromadb>=0.4.0             # Vector DB
sentence-transformers>=2.2.0  # BGE-M3 local embeddings

# Application & Tools
streamlit>=1.30.0       # UI Dashboard
python-dotenv           # API Key management
requests                # API handling
pandas                  # Data handling
pydantic>=2.0.0         # Data models/validation
