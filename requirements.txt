# Streamlit Community Cloud deployment
# EasyOCR + ultralytics are NOT here — they load large PyTorch models that OOM
# the 1GB Cloud limit. All OCR on Cloud routes through xAI Vision API instead.
# Full local stack: requirements-full.txt

numpy>=1.21,<3
pillow
pillow-heif

# AI & LLM
openai>=1.0.0            # xAI / Grok API client

# RAG & Vector DB
chromadb>=0.4.0          # Vector store
sentence-transformers>=2.2.0  # Cross-encoder reranker

# Web scraping (DDInter client)
beautifulsoup4>=4.12.0
requests>=2.31.0

# Application
streamlit>=1.30.0
python-dotenv
pandas
pydantic>=2.0.0
