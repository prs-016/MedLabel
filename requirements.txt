# Streamlit Community Cloud deployment
# ultralytics is NOT here — YOLO runs via Roboflow REST API instead (no local model).
# EasyOCR is now included; it previously OOMed only when combined with ultralytics.
# Full local stack: requirements-full.txt

numpy>=1.21,<3
pillow
pillow-heif
opencv-python-headless>=4.8,<5

# OCR
easyocr>=1.7.0               # Flat label OCR (no ultralytics = fits in 1GB Cloud)

# AI & LLM
openai>=1.0.0            # xAI / Grok API client

# RAG & Vector DB
chromadb>=0.4.0          # Vector store
sentence-transformers>=2.2.0  # Cross-encoder reranker

# Web scraping (DDInter client)
beautifulsoup4>=4.12.0
requests>=2.31.0

# Application
streamlit>=1.30.0
python-dotenv
pandas
pydantic>=2.0.0
